from html.entities import name2codepoint
import nturl2path
from tkiter import
def suma():
    n1=float(entry1.get())
    n1=float(entry2.get())
    rto=n1+n2
    LabelRto.config(text=rtotext)
ventana=Tk()
entry1=entry(ventana)
entry2=entry(ventana)
Labelrto.pack()